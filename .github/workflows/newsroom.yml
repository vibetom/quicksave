# Runs the autonomous newsroom on a schedule and deploys to GitHub Pages.
# Fires 5 times a day inside the publish window = 5 articles/day.
# Required repo secret: GEMINI_API_KEY

name: autonomous-newsroom

on:
  schedule:
    - cron: "30 8,11,14,17,20 * * *"  # 5 runs/day (UTC)
  workflow_dispatch: {}

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: newsroom
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - name: Produce one article and rebuild site
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python -m agents.pipeline once

      - name: Commit new content
        run: |
          git config user.name "quicksave-bot"
          git config user.email "bot@users.noreply.github.com"
          git add content public
          git diff --cached --quiet || git commit -m "auto: publish article"
          git push

      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    needs: publish
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
